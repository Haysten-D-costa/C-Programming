//CP COMPILE TIME INITIALIZATION...and display using printf statements
//HAYSTEN D'COSTA(21CO56)
#include<stdio.h>
int main()
{
    //Declaration and initialization of variables(ARRAY)
    int A[5]={2,4,6,8,10};

    //Output using printf statement
    printf("Elements in an array are as follows: ");
    printf("\n\nValue of first element in array = %d",A[0]);
    printf("\nValue of second element in array = %d",A[1]);
    printf("\nValue of third element in array = %d",A[2]);
    printf("\nValue of fourth element in array = %d",A[3]);
    printf("\nValue of fifth element in array = %d",A[4]);

    return(0);
}
